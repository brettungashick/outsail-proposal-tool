DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="generate-a-secure-random-string"
NEXTAUTH_URL="http://localhost:3000"
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# Turso (production only — leave blank for local SQLite dev)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# SMTP (for invite emails, password resets — leave blank to disable email sending)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="OutSail <noreply@outsail.co>"
